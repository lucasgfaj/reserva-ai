{
  "version": 2,
  "buildCommand": "npx prisma generate && npx nest build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/v1/(.*)", "dest": "/api" },
    { "src": "/api/v1", "dest": "/api" }
  ]
}
